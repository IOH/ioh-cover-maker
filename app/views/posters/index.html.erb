<!-- navbar  -->
<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container-fluid">
	  <!-- Brand and toggle get grouped for better mobile display -->
	  <div class="navbar-header">
	    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
	      <span class="sr-only">Toggle navigation</span>
	      <span class="icon-bar"></span>
	      <span class="icon-bar"></span>
	      <span class="icon-bar"></span>
	    </button>
	    <a class="navbar-brand" href="./">IOH Cover Maker</a>
	  </div>

	  <!-- Collect the nav links, forms, and other content for toggling -->
	  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
	    <% if current_user %>
				<ul class="nav navbar-nav">
	      	<li class="active"><a href="./">Poster</a><span class="sr-only">(current)</span></a></li>

		    	<li><a href="#">問與答</a></li>
		   	</ul>
		    <%= form_tag search_path, class: 'navbar-left navbar-form',id: 'search-form', remote: true do %>
				<div class="form-group ">
					<%= text_field_tag 'query', nil, placeholder: 'Search', id: 'search-text_field', autocomplete: 'off', class: 'form-control'%>
				</div>
				<%= submit_tag 'Go', id: 'search-submit', class: 'btn btn-default' %>
			</form>
		  <% end %>
	      <ul class="nav navbar-nav navbar-right">
	        <li><%= link_to current_user.account_name, "#" %></li>
 			<li><%= link_to "logout", logout_path, method: "delete" %></li>
	      </ul>
	    </div><!-- /.navbar-collapse -->
	  </div><!-- /.container-fluid -->
	</nav>


<div class="container" style="padding-top: 70px;">
  
	<%= link_to 'Create New Poster', new_poster_path, class: 'btn btn-default' %>

	<div class="posters">
		<% @posters.each do |poster| %>

			<h2><%= poster.name %></h2>
			<%=  %>
			<%= link_to 'Edit', edit_poster_path(poster) %>
			<%= link_to "Delete", poster_path(poster), method: "DELETE", data: { confirm: "確定要刪除 #{poster.name} 嗎？" } %>
			<hr>
  	<% end %>
  </div>

	<% end %>
</div>

<%= javascript_include_tag 'posterIndex', 'data-turbolinks-track' => true %>